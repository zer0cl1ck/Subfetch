# Subfetch
Subfetch is a lightweight and efficient subdomain enumeration tool designed to simplify the process of gathering subdomains from multiple reliable sources in a single run. It was created to reduce the manual effort of running several tools separately and then cleaning, merging, and deduplicating their outputs.
